/**
 * Shared constants used across the PDF editor.
 */

/** Minimum width/height for text box annotations (PDF units). Keeps boxes usable (e.g. resize handles) without forcing a large minimum. */
export const MIN_TEXT_BOX_SIZE = 8;
